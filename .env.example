# =================================
# 占いの小窓 - 環境変数設定（本番環境用）
# =================================

# ===== 必須設定 =====

# アプリケーション設定
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://your-app-domain.com  # 必須：実際のドメインに変更

# セキュリティ設定（必ず変更してください）
JWT_SECRET=your_jwt_secret_minimum_32_chars_CHANGE_THIS  # 必須：32文字以上のランダム文字列
SESSION_SECRET=your_session_secret_32_chars_CHANGE_THIS  # 必須：32文字以上のランダム文字列
ADMIN_PASSWORD=secure_admin_password_CHANGE_THIS  # 必須：強力なパスワードに変更

# ===== LINE設定（LINE機能を使用する場合） =====

# LINE Login API（ユーザー認証用）
# https://developers.line.biz/console/ → LINE Login Channel
NEXT_PUBLIC_LINE_CHANNEL_ID=  # Channel ID
LINE_CHANNEL_SECRET=  # Channel Secret
NEXT_PUBLIC_LINE_REDIRECT_URI=https://your-app-domain.com/api/auth/callback/line

# LINE Messaging API（チャット・通知用）
# https://developers.line.biz/console/ → Messaging API Channel
LINE_MESSAGING_CHANNEL_ACCESS_TOKEN=  # Channel Access Token
LINE_MESSAGING_CHANNEL_SECRET=  # Channel Secret

# ===== Stripe設定（決済機能を使用する場合） =====

# https://dashboard.stripe.com/apikeys で取得
STRIPE_SECRET_KEY=sk_live_  # Secret Key（sk_live_で始まる）
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_  # Publishable Key（pk_live_で始まる）
STRIPE_WEBHOOK_SECRET=whsec_  # Webhook Endpoint Secret

# ===== OpenAI設定（AI機能を使用する場合） =====

OPENAI_API_KEY=sk-  # OpenAI APIキー
OPENAI_MODEL=gpt-4o-mini  # 使用するモデル

# ===== オプション設定 =====

# JWT有効期限
JWT_EXPIRES_IN=7d

# ログレベル（ERROR, WARN, INFO, DEBUG）
LOG_LEVEL=INFO

# レート制限（1分あたりのリクエスト数）
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=60

# デモモード（本番環境では必ずfalseに）
DEMO_MODE=false

# ===== データベース設定（将来の拡張用） =====
# DATABASE_URL=postgresql://username:password@host:port/database_name

# ===== Redis設定（セッション管理・キャッシュ用） =====
# REDIS_URL=redis://localhost:6379

# ===== メール通知設定 =====
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# =================================
# セキュリティチェックリスト：
# ✅ JWT_SECRET を変更した
# ✅ SESSION_SECRET を変更した
# ✅ ADMIN_PASSWORD を変更した
# ✅ NEXT_PUBLIC_APP_URL を実際のドメインに設定した
# ✅ 本番用のAPIキーを設定した
# ✅ DEMO_MODE を false に設定した
# =================================

# =================================
# 設定手順：
# 1. このファイルを .env または .env.local にコピー
# 2. 必須項目をすべて設定
# 3. 使用する機能に応じてオプション項目を設定
# 4. セキュリティチェックリストを確認
# 5. .env/.env.local は .gitignore で除外されていることを確認
# =================================